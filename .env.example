# Copy to .env.local and fill in from your Supabase project:
# Dashboard → Project Settings → Data API (URL) and API Keys (anon / publishable).
#
# Both are safe to expose in the browser bundle — the anon key only grants what
# your Row Level Security policies allow. Never put the service_role key here.

NEXT_PUBLIC_SUPABASE_URL=https://ltysyhwyzqbumvdpumxq.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
